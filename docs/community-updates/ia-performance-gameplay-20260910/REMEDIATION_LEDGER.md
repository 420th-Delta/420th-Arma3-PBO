# Remediation ledger

This is the running record for the authorized remediation and local-test campaign. It distinguishes mission defects from test/infrastructure defects. Original findings and failed evidence remain preserved; an implemented fix is not automatically a verified fix. Cell identifiers resolve under the Server Lab's ignored `artifacts/ia-update/` directory. Raw logs stay private; committed summaries omit player identities and configuration secrets.

## Mission issues

| ID | Finding and consequence | Remediation | Evidence / current verification |
| --- | --- | --- | --- |
| SUP-1 | CfgFunctions invokes `postInit`, but the support observer accepted `INIT` only. Role menus never started normally. | Normalize initialization; latch one observer and retain lifecycle handler IDs. | Real client observer, idempotence, incapacitation/recovery and native control transitions pass in final `b11ac9b1b0a4-support` (207/207, zero errors). |
| SUP-2 | Virtual projectile null source bypassed on-foot friendly damage reduction and Hit accountability gates despite a valid instigator. | Set both shot parents to caller; resolve surviving instigator in both consumers. | Confirmed in `b5094ad22f2c-damage`; final focused matrix `f57c8d30a524-damage` passes 98/98 with zero errors, covering all eight profiles and 5,286 eligible foot callbacks with zero modifier failures across native descendants and both attribution variants. Real player/Zeus and complete Hit accountability UI remain separate acceptance tests. |
| SUP-3 | Mortar success ACK could grant ammunition without spending a tube; scheduled requests could interleave. | Reserve exact container; keep pending mortar empty; server observes consumption before arming; serialize transitions. | Final `b11ac9b1b0a4-support` passes 207/207: scheduled concurrency, forged/replayed/late ACK, cancel/reset/timeout/substitution, ordinary placement, three-mortar cap, free request and artillery stock/refund/epoch/UID persistence. Full placement UI and distinct-player permissions remain separate acceptance tests. |
| SUP-4 | Both support menu calls supplied Boolean FALSE as BIS addCommMenuItem's notification-class string. | Pass empty notification name. | Native error in `7249c16a2b31-support`; final `b11ac9b1b0a4-support` has valid menus and zero script errors. |
| SUP-5 | `isNil` loses remote execution metadata; recursive atomic dispatch rejected legitimate scheduled requests. | Capture engine caller provenance before an atomic lexical server dispatch in both APIs, including cancellation/refunds. | Exact before/after context traced in `0f65cd3133b8-support`; authenticated scheduled concurrency and artillery cancellation/refund pass in final `b11ac9b1b0a4-support`. |
| SUP-6 | Delayed by-unit global backpack removal could consume a replacement backpack. | Authenticated owner-local DEBIT atomically matches exact reserved container; server independently waits for its deletion. | Worn-container debit and delayed-packet preservation of replacement contents and the dropped original pass in `429136a53def-support` and final `b11ac9b1b0a4-support`. Unsupported exact `deleteVehicle` probe preserved as failed evidence. |
| SUP-7 | Numeric-target setVariable updates the client only. Comparing the server's absent copy could suppress later empty-status publication. | Server owns a body/owner publication cache and publishes transitions, including clears. | Native semantics exposed by `429136a53def-support`; actual status delivery and clearing through activity end, role departure and rejoin pass in final `b11ac9b1b0a4-support`. |
| SUP-8 | Vertical LASER/IR carrier shells create native children with nonfinite position/direction, fail guidance and never hit. | Resolve the configured missile child directly; validate its simulation; do not debit a release whose target assignment fails. | Ten-mode native comparison `fca96e76c6f3-guided`: direct children hit with protection; eight carrier variants fail. Exact-production matrix `f57c8d30a524-damage` passes all eight profile releases, finite flight, impacts and protection with zero errors. Complete worker/integrated snapshot acceptance remains separate. |
| SUP-9 | Ordinary immediate IR missile target assignment can reject a valid enemy designation, cancelling otherwise valid support. | Use forced assignment after authoritative designation validation; require a live, non-null designation at the release transition and keep rejected rounds reserved for refund. | `efaecad72cd8` reproduces rejection; focused `64d3eeae9acd-guided` has one expected unforced-IR negative while forced immediate IR retains its target, flies finitely and hits. Final focused matrix `f57c8d30a524-damage` passes 98/98, including dead/null designation rejection, with zero errors. Integrated snapshot acceptance remains separate. |
| SUP-10 | A newly created mortar reports owner 0 and rejects immediate setOwner, so PLACE receives its correct nonce but never gains locality and cancels after eight seconds. | Reserve first, then retry transfer in the existing core sweep once the engine registers an owner; send PLACE and accept ACK only after transfer succeeds. Transfer and handoff each have one bounded 15-second window. | `2802730db519-support` captures false transfer/owner 0 and correct-nonce/nonlocal client timeout. Final `b11ac9b1b0a4-support` passes ownership, pre-handoff ACK rejection and ordinary placement/section tests at the real three-second cadence; first debit/arm takes 6.301 seconds. |
| AI-01 | Final-reserve rotary aircraft were counted but not owned by AO teardown. | Register hull and every crew member during creation, including cancellation paths. | Final focused `9159954efb59-ai-spawn` passes aircraft/crew registration, normal STOP, native deletion/provider pruning, cancellation ownership and eventual worker termination. All 23 suite checks pass with zero script errors and clean cleanup/source/fixture/active-copy checks; full mission acceptance remains separate. |
| AI-02 | Exempt helicopter hull did not exempt owned crew when STOP selected deletions. | Check the parent vehicle exemption for crew members. | Final focused `9159954efb59-ai-spawn` passes the imported negative case and corrected hull/crew preservation; full mission acceptance remains separate. |
| SYS-02 | Displaced group slots could violate the caller's player/base exclusions; unconditional expansion changed mandatory legacy objective spawning. | Audit all 72 callsites; opt in wider placements with final-position validators; retain compact mandatory legacy calls; use server-owned short atomic claim commits after scheduled searches. | Final focused `9159954efb59-ai-spawn` passes boundary/spacing, mandatory legacy guards and imported negative, expired/stale claims and concurrent scheduled requests. All 23 suite checks pass; full mission performance and HC acceptance remain separate. |
| AI-03 | GRID AI dispatch ran on HCs as well as server, allowing duplicate activity spawning from broadcast triggers. | Apply the existing server-only dispatch policy to GRID. | Static call-path review; controlled full CLASSIC mission with one HC passes 28/28 in `d3ae2b632fb3`, including real HC registry/object agreement and native offload. GRID-mode gameplay remains separate acceptance. |
| AI-04 | Ambient spawn blacklist test checked the target rather than the spawn position. | Check the actual candidate and displaced slots. | Static callsite audit; placement regression covers final validators. |
| AI-05 | FOB anchor search had no finite attempt bound. | Limit attempts and defer when no valid anchor exists. | Static review and caller failure-path audit. |
| AI-06 | Embedded rappel helper strings contain comments that fail raw `compileFinal` compilation during mission initialization. Outer-file compilation did not execute those strings. | Convert five affected helpers to `compileFinal` Code input, preserving decoded logic and provenance; audit remaining AI/insertion literals. | Five helper failures captured in full mission `fe683de5fe31-integration`. Exact decoded-body, delimiter and remaining-comment-string checks pass. Controlled full-mission cells `8ff629b6241a` (0 HC, 22/22) and `d3ae2b632fb3` (1 HC, 28/28) initialize all tested roles without script errors. Actual insertion/descent remains separate acceptance. |
| SYS-01 | Eleventh custom radio channel requires 2.22; allocation failure disabled Side communication. | Native team-local Side fallback and allocation diagnostic. | Native client fallback/access assertions exercised on 2.22; actual deployment version remains a separate check. |
| SYS-03 | Staff channel mask controls omitted the new Side channel. | Include the actual custom Side ID or native fallback in access/menu masks. | Actual GUI control creation and mask preservation pass in `a4a3ba8fe16e-radio-cleanup`. |
| SYS-04 | Deferred Kavala terrain restoration could unhide terrain belonging to a restarted activity. | Generation-check restoration and explicitly hide terrain on entry. | Stale/current-generation native helper checks pass in final focused cell `3e45c5f9b45c-radio-cleanup`; full restarted Kavala lifecycle remains separate. |
| SYS-05 | Deferred Kavala deletion could act on newly occupied, protected or attached objects. | Recheck current players, captives, crews, transport and protection immediately before deletion/recycling. | Native protection, captive, attachment and proximity predicates pass in final focused cell `3e45c5f9b45c-radio-cleanup`; full collector/HC ownership lifecycle remains separate. |
| SYS-06 | Later inventory additions inherited an arsenal holder's old cleanup deadline. | Compare cargo snapshots and renew the 30-second deadline after observed changes. | Actual native tick renewal, unchanged-cargo deletion and attachment checks pass in final focused cell `3e45c5f9b45c-radio-cleanup`. Remote player Put timing and nested contents of an already dropped backpack remain documented limits. |
| SYS-07 | Valid four-argument radio respawn removal/addition leaves the exact old body in General's native roster. | Restore the replacement synchronously, then use a bounded worker to observe replacement membership and remove only the captured old body across engine frames. | Reproduced in `468e715589c1`; callback signature is correct and isolated removal succeeds. `c2c6272afcef` passes production lifecycle outcomes but fails two reused-object setup checks (TEST-16). Final isolated fixture `3e45c5f9b45c` passes 70/70 with zero errors; genuine death/respawn remains separate. |
| SYS-08 | Baseline initialization and periodic client refresh query vehicle sound controllers on an on-foot human, producing native warnings every five to eight seconds. | Guard null/human objects at both callsites; retain real-vehicle and environmental controller data. Keep this correction in a separate baseline commit. | `a599b201e269` records 18 warnings; both calls predate the import (`a99001f`). Official API excludes humans. Controlled full-mission `8ff629b6241a` passes 22/22 with zero such warnings across on-foot initialization and 119.535 seconds of admitted-client heartbeats; boarding/dismount remains separate. |
| SYS-09 | Fresh mapped Cargo HQ and windmill objects remain at temporary off-map creation positions on an already connected client, while the server places them correctly. A direct-created Cargo HQ control is positioned correctly. | Create fresh mapped Houses on land at their final position; publish the post-callback orientation in the existing authenticated, object-bound snapshot. Keep vehicle, water and simple-object creation behavior intact. | Baseline probes reproduce the client mismatch, including after 15 seconds. Position-only and position-refresh candidates leave client tilt incorrect. Final `efdcf250f5be-hq-delete` passes 282/282 with zero script, cleanup, malformed-record or integrity errors: two graphical clients, genuine late-join replay, native placement/orientation/geometry, later snapshot replacement and forged-client rejection. Exact tested mapper bytes applied (SHA-256 `6506ec48e2153d843fa82826fb94b82512f8b5d06803dae8e09c516159110284`). Final full two-HC cycle `7a98937cbfa8-integration` passes 45/45 with all strict gates clean and all 916 source files matching the final snapshot. |

SYS-09 also requires the final House orientation to survive the entity's existing client-state snapshot. The publisher/receiver pair now carries an optional fifth field containing two finite three-component vectors for non-simple Houses. The mapper captures the original House before its callback and publishes its resulting orientation afterward, including when that callback returns an array. Existing feature, face and spawn-handler fields remain in the same object-bound JIP entry; the receiver's server-origin guard remains intact. Native current-client and late-join verification passes in `efdcf250f5be-hq-delete`. Feature-field evidence establishes authenticated snapshot transport and execution of the production setter, not visual display rendering.

Independent SYS-09 source review found a snapshot-ordering race: a scheduled publisher could read old state, be suspended while a newer snapshot is published, then enqueue the old snapshot last. The publisher now performs its fixed-size read, validation and enqueue in one lexical `isNil` transition, with no recursion or added worker. It preserves the previous publication-result-or-nil return contract; the three existing production callers were audited. This race was identified from scheduler behavior and source ordering, not a separate native failure reproduction. Later authored rotations must update the stored vectors before republishing. An absent vector field does not reset orientation; deletion retains the existing engine-managed object-bound JIP cleanup. The exact publisher and receiver pass the 282-check native cell with current and later snapshot publication; source review establishes the bounded atomic ordering.

## External content issues

| ID | Issue | Disposition / evidence |
| --- | --- | --- |
| EXT-1 | Stock Nyx Scout MFD condition `(ammo1+ammo2)` raises undefined `ammo1` during object creation, including inherited decorative startup. | Full-mission `a599b201e269` remains a strict failure. Existing independent stock native reproduction isolates the Tank PBO condition; its installed hash matches the pinned evidence. No mission source fix or error suppression. An explicit optional `reviewed-scout-v1` altered-content control validates and privately stages the existing reviewed addon, with native patch/condition/source/build assertions in every role. Eight staging checks pass in `acc4e4306e5e-mfd-stage-check`; controlled native results are separate. See [investigation](stock-mfd-investigation.md). |

## Test and infrastructure issues

| ID | Issue | Correction / evidence |
| --- | --- | --- |
| TEST-1 | Sandboxed graphical client exited with code 53 before producing an RPT. | Use the approved host execution path with hidden clients and the same owned-process safeguards. First attempt `0f27bfd5d43b-support` remains failed. |
| TEST-2 | SQF preprocessing stripped single-quoted comment markers; raw compiled extracted source also retained comments. | Extract exact hashed blocks into files and use native preprocessing. Failed radio cells `90858ad1230a` and `a4a3ba8fe16e` retained; all native cleanup checks run without script errors in `c2c6272afcef`. |
| TEST-3 | Staff GUI fixture invoked namespace-sensitive GUI code scheduled rather than in its normal atomic UI context. | Invoke through `isNil`; actual GUI controls and masks then pass. |
| TEST-4 | Old-body radio fixture used a remote group; server's client-completion wait was shorter than client startup plus tests. | Use an owned local group, bounded replication waits and a longer completion deadline. Complete client execution and original old-body General removal pass in `c2c6272afcef`; separate case-isolation correction is TEST-16. |
| TEST-5 | Cancellation assertion expected `terminate` to stop a worker immediately. | Wait through scheduler processing; separately assert immediate cleanup ownership and no later additions. |
| TEST-6 | Git ownership differs between sandbox-created worktree and escalated Windows user. | Baseline extraction applies read-only, command-scoped trust to the exact verified checkout; no global Git configuration changes. |
| TEST-7 | Full-mission native PBO cache exceeded the small synthetic profile bound. | Budget known mission-cache size separately; retain 64 MiB RPT cap. Initial `16d4cae16afd-integration` aborted and cleaned up. |
| TEST-8 | Integration needed explicit private Custom difficulty options and active-mission hash checks. | Set required options in private profile; verify frozen fixture and launched mission contents after execution. |
| TEST-9 | Test RPC called production TICK while still in remote execution context. | Independently started server loop owns periodic TICK. Preserve production's remote lifecycle rejection. |
| TEST-10 | Artillery fixture read client-targeted status on the server, yielding empty arrays and secondary assertion errors. | Read authoritative server publication cache and separately wait for actual client delivery. |
| TEST-11 | Large aggregate damage record exceeded the engine's RPT line limit. | Emit bounded per-key records; parser reports malformed records rather than hiding them. First probe's individual event evidence remains usable. |
| TEST-12 | Early guided fixture lacked a valid enemy IR designation; collision damage could satisfy a broad hit counter. | Use occupied, heated, revealed enemy targets and count nonempty-ammunition events separately. |
| TEST-13 | Diagnostic distance calculations on nonfinite guided projectile coordinates caused extra script errors. | Record finite flags and unavailable distance without applying spatial commands to invalid coordinates. Original failed guided cell retained. |
| TEST-14 | Full mission's interactive lobby waits for manual role selection, so a connected automated client did not enter the mission. | Apply the existing lab runner's private `skipLobby=1`/`joinUnassigned=0` role-entry automation and document this test-only difference. Source settings stay intact. |
| TEST-15 | Holder cleanup and rejected-guidance fixtures expected `deleteVehicle` to yield `objNull` within the same frame. | Bohemia documents deletion on the next frame. Assert the immediate counter/queue or reserved-round transition separately, then wait up to one second for actual object deletion. `468e715589c1` and `efaecad72cd8` retain their failed immediate observations. Corrected rejected-guidance checks pass in `f57c8d30a524`; holder deletion passes in `c2c6272afcef` and final `3e45c5f9b45c`. |
| TEST-16 | Radio lifecycle cases re-added the same old body while an earlier bounded retirement worker could still remove it; two registration preconditions failed despite successful production outcomes. | `c2c6272afcef` retains 70 checks, two setup failures and zero script errors. Each case now owns a fresh old object, waits for native membership and records the individual precondition Booleans. Assertions remain strict; isolated native retest `3e45c5f9b45c-radio-cleanup` passes 70/70 with zero errors or provenance/cleanup failures. |
| TEST-17 | Ordinary integration telemetry read undeclared `QS_ao_active`; its final check only re-read the initialization latch, and an HC registry count did not establish current HC objects or offload. | Use actual `QS_classic_AI_active`; both observation and cycle now verify real humans, HC objects with matching registry owners, and active Primary/Defense controllers at completion. Record per-owner AI-unit/group counts as diagnostics without equating registration with offload. Native controlled 0-HC and 1-HC cells pass 22/22 and 28/28; the latter observes 29 AI on the HC and 93 on the server. |
| TEST-18 | Adding HC mod junctions to the private launcher did not add them to the independent fallback cleanup's expected-target inventory. An interrupted launcher could leave HC links attached. | Apply matching HC roles and server-mod selection to generated fallback cleanup. Before any HC control run, all six generated launcher/cleanup scripts for zero, one and two HCs pass PowerShell AST parsing; role and mod selection agree for zero, one and two players. Existing process, ancestry, exact-target and junction-only deletion guards remain intact. No engine was launched for this check. |
| TEST-19 | The full-cycle fixture required every deleted HQ handle to become null. Native full-mission House handles can remain non-null after physical/network deletion. | Preserve the original failed cycles. Require ordinary objects to become null; accept a retained House only after valid original network/world/nearest/geometry presence, an observed Deleted event, owner 0, null original-ID lookup, and fresh world/nearest/geometry absence. `4f1ab1639be5` passes 45/45 with all other gates clean: 39 null objects and six strictly observed retired Houses. No production cleanup change was needed. |

## Investigation disposition

| ID | Observation | Next verification |
| --- | --- | --- |
| INV-1 | Two-HC forced-Defense/cancellation cells `ebf08d51fc72` and `2bd3366547c5` each pass the activity transitions but fail old-HQ all-null and aggregate cycle assertions (45 checks, two failures, zero script/cleanup/integrity errors). | Closed as TEST-19 by direct full-mission deletion evidence in `4f1ab1639be5`. Two isolated probes deleted all eight handles normally, even after the Defense building-position queries; the exact engine-internal reason for retained full-mission handles is not established. Those probes independently exposed SYS-09 client placement, which remains a separate mission issue. |

## Reading the results

Subsystem details: [support](support-remediation.md), [damage](damage-remediation.md), [guided investigation](guided-native-investigation.md), [AI/spawning](ai-spawn-remediation.md), [radio/cleanup](radio-cleanup-remediation.md), [baseline client audio](client-audio-remediation.md), [HQ deletion and mapper synchronization](hq-native-investigation.md). Reproduction and containment: [native test README](../../../tests/ia-update/README.md).

Local Windows engine tests, graphical fixture assertions, full-mission startup/HC execution, and human/live-server acceptance are separate evidence categories. No performance gain, independent-account permission coverage or live deployment acceptance is claimed from these local cells. The authorized local remediation campaign is complete: 25 mission findings have corrections or compatibility handling recorded, 19 test/infrastructure findings have dispositions, and the latest five focused suites and final controlled full cycle pass. EXT-1 remains an external stock-content issue; the human/deployment acceptance limits in the test report remain explicit. All 40 attempts are preserved in the sanitized campaign index.
