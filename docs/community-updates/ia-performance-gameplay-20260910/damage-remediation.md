# Damage attribution remediation

SUP-2 is confirmed by native Windows Arma 3 2.22 execution, not just source inspection. The first dedicated probe (`b5094ad22f2c-damage`) supplied the exact eight production ammunition classes with either null-source/caller-instigator or caller-source/caller-instigator parents. Actual HandleDamage events from HE, rockets, Mk82, GBU-12, ICM and CBU-85 reported a null source with the correct friendly instigator in the first variant. The production on-foot modifier returned 0.925; the caller-source variant returned the intended zero. Native cluster children inherited both parent identities. Probe victims and callers were server-local AI, and raw damage was returned unchanged to preserve event observation.

The correction sets both shot parents to the caller and accepts a resolved instigator when the damage source is null. The Hit handler resolves a missing source before its crew/accountability checks. Physical firing-side resolution remains in the existing shared helper; source-as-victim collision handling is preserved.

The corrected probe (`1415253202bc-damage`) recorded zero protection failures for the captured friendly on-foot projectile events, including thousands of ICM/CBU events. Direct production-function checks cover enemy physical source with a WEST instigator, a null friendly source, self-collision and unattributed damage. That historical probe failed overall because the guided LASER/IR carriers did not produce the required impacts. Native follow-up isolated the corrupt carrier transition and intermittent first-frame IR target rejection as SUP-8 and SUP-9; the [guided investigation](guided-native-investigation.md) preserves the failed variants and their corrections.

Final focused cell `f57c8d30a524-damage` passes **98/98 assertions** on Windows Arma 3 2.22.0.154045 with zero script errors, malformed records, cleanup errors or changed frozen/deployed files. It executes exact extracted production profiles, ammunition resolution and atomic release statements. All eight profiles produce finite sampled flight and native ammunition impacts in both caller-source and deliberately restored null-source variants. Sixteen protection cases evaluate 5,286 eligible on-foot callbacks with zero modifier failures, including native ICM/CBU descendants. Dead and missing guided designations both delete the rejected projectile without spending the reserved round. The earlier `cb04dc759432-damage` passed 97 assertions before the missing-designation guard/test was added.

The complete support worker is tested separately: `2802730db519-support` confirms one real JTAC Mk82 release, then rejects a second release when the actual player moves onto its previously safe target and refunds exactly once. Its remaining mortar placement failures are unrelated to that artillery result and remain recorded in the support report. These results do not establish moving-target guidance or the accuracy of the conservative impact-time estimate.

Private cell directories under the Server Lab's ignored `artifacts/ia-update/` contain frozen source hashes, projectile events, RPTs and cleanup evidence. The initial probe's final aggregate record exceeded Arma's RPT line limit; individual earlier records remain readable, and subsequent probes emit bounded per-key summaries. Actual player-facing Robocop UI, controller locality and sustained multiplayer acceptance remain distinct from dedicated AI attribution evidence.
