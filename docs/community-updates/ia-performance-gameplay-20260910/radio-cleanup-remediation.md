# Radio and cleanup remediation

This records the radio/cleanup correction pass after `dec9787`, including native fixture failures and subsequent corrections. The original review remains a record of the imported contribution. Native outcomes below distinguish completed evidence from retests still required.

| Finding | Correction | Acceptance evidence required |
| --- | --- | --- |
| SYS-01: custom Side allocation can return zero after the ten existing channels | Restore native Side text and voice when custom allocation fails. Record the degraded mode in the server RPT and radio menu. Native Side communicates within the player's team; its receive membership cannot be individually disabled. Successful custom allocation retains optional cross-team subscription. | Genuine client channel permissions and membership on 2.22; forced zero-result path on that build. Running the fallback on a pre-2.22 binary is a separate compatibility test. |
| Staff cannot manage the new Side channel | Add its actual mapped UI ID to the existing staff channel-mask GUI; use native channel 1 in fallback mode. Existing private Staff and General permissions remain independent. | Actual GUI enumeration, Side voice mask and mask persistence after access refresh. VON audio delivery requires speaking/listening participants. |
| Kavala restart toggles terrain visible while an earlier restore is pending | Hide explicitly on start and stamp restoration work with the activity generation. The collector retires obsolete generations without changing visibility. | Exact production restore helper tested with stale/current generations; full interrupted/restarted Kavala still requires mission integration. |
| Kavala asset safety is only checked when queued | Use `KAVALA_DISCREET` work with fresh player, captive, attached/transported cargo and crew checks at the actual delete/recycle transition. Preserve recycler integration and cleanup counters/timing. | Production predicate checks capture/attachment/protection and fresh player proximity. Full collector execution and HC-owned crews require integrated execution. |
| New drops in an existing arsenal holder inherit its original expiry | Compare actual server cargo snapshots on each bounded cleanup pass; a change renews the 30-second deadline. Snapshot read, final protection/transport checks and deletion run together. The player notice describes inactivity expiry. | Execute the actual housekeeping tick against real holders: late additions survive, unchanged expired cargo is removed, attached holders survive. A real remote player Put and replication timing remain separate tests. |
| SYS-07: same-frame radio replacement retains the old body in General | Restore the replacement immediately, then observe its native membership and remove only the captured old body in a bounded three-second worker. | Final isolated fixture `3e45c5f9b45c` passes all lifecycle preconditions and outcomes; real death/respawn remains separate. |

Cargo observation uses weapon, magazine, item, backpack and weapon-attachment contents. Expiry is measured after the last **observed** change; a take-and-replace operation that produces an identical snapshot between passes cannot be distinguished. This avoids trusting a client-provided timestamp and retains bounded server work. Inventory changes inside a backpack already lying in a holder are not individually timestamped. The holder itself remains the cleanup unit.

The crate cleanup already performs a fresh player and linked-equipment check inside an unscheduled final transition. Its existing five-minute absence rule was retained. Tests cover its actual time-budget/absence predicates; landed-aircraft, unconscious-player and final-frame return behavior require genuine player/vehicle observations. The ruin event classification was inspected and no concrete new defect was established in this pass.

The radio correction preserves the donor entitlement cache input, private Staff eligibility, General's mandatory membership and upstream four-argument exact-body respawn/killed handling. Complete cross-team Side needs Arma 3 **2.22+** and a free custom slot: Bohemia documents ten slots before 2.22, fifty afterward, zero on allocation failure, and the extra UI offset for custom slots above ten. This fallback does not declare the entire overhauled mission compatible with every older engine. References: [radioChannelCreate](https://community.bistudio.com/wiki/radioChannelCreate), [Channel IDs](https://community.bistudio.com/wiki/Channel_IDs), [enableChannel](https://community.bistudio.com/wiki/enableChannel), [radioChannelInfo](https://community.bistudio.com/wiki/radioChannelInfo). These were checked through indexed official documentation on 2026-09-10; direct wiki page requests returned HTTP 403.

Fixtures under `tests/ia-update/`:

- `radio_cleanup_extract.py` stages the exact marked production housekeeping helper block and tick under `tests/radio-cleanup-extracted/`, with source and slice SHA-256 metadata. `radio-cleanup-server.sqf` preprocesses those files and `radio-cleanup-server-body.sqf` before compiling them together; the body supplies native objects and assertions. No copied cleanup implementation is used. It loads the actual `code/config/QS_data_listVehicles.sqf` with its normal class-list cache, creates radio channels 1–11 and publishes readiness for the client fixture. The cleanup arena is `[14500,16000,0]` on Altis and must be clear of real players for eligible-deletion assertions.
- `radio-cleanup-client.sqf` compiles the actual radio, staff GUI and database-whitelist reader functions against controlled whitelist data, then checks custom/native Side, membership, opt-out, donor expiry, staff GUI/masks and exact-body lifecycle calls on a real client. Calling lifecycle functions against an AI old body does not constitute a genuine death/respawn test.
- Both use `IA_fnc_assert`, `IA_fnc_log` and `IA_fnc_finish`. Package the mission source as `production/`, fixture files as `tests/`, and run them in an isolated test mission. The fixtures alter mission namespaces, channels and test entitlement data; they are not live-server diagnostics. No extra test remote-execution endpoint is needed for these scripts.

Static verification for this pass: `git diff --check` on the initial seven changed production files passed (Git emitted only the checkout's normal LF/CRLF conversion warnings). Native SQF compilation and graphical/dedicated execution are delegated to the shared serial test lane. No Arma processes were started and no commits were created by this remediation subtask.

First native cell `90858ad1230a-radio-cleanup` exposed two fixture defects: preprocessing interpreted the literal comment prefix inside a single-quoted extraction anchor, and the scripted client invoked the namespace-switching staff GUI from scheduled code instead of a UI event's unscheduled context. The revised fixture constructs comment prefixes numerically, enters the actual GUI through `isNil`, and checks all controls were created. Bohemia documents the [namespace switching/scheduler issue](https://community.bistudio.com/wiki/with). The same cell passed native Side fallback, custom opt-out, donor revocation and staff masking checks, but old-General-body removal failed; that lifecycle check now uses a separate local AI group, waits for membership propagation, and records exact rosters plus isolated native removal if it fails again. These changes are test corrections/diagnostics, not evidence that the remaining lifecycle failure is resolved.

Cell `a4a3ba8fe16e-radio-cleanup` confirmed the actual staff GUI creates all controls in unscheduled context, Side masking persists, and the local old body registers with the expected General/Side rosters. Raw compilation of source slices containing comments still failed on the server. The extractor now writes exact slices as files so the engine preprocessor handles the original comments; SQF no longer searches source markers or compiles raw `loadFile` text. Early server failure plus the harness's former 30-second client grace period truncated the remaining lifecycle check. The harness owner raised that grace period to 90 seconds. Both failed evidence directories remain unchanged. Their cleanup compilation and completion issues were resolved by the subsequent cells described below.

Fixture issue accounting:

| Issue exposed by native testing | Correction | Retest status |
| --- | --- | --- |
| SQF preprocessor treated the single-quoted source-marker prefix as a comment | Moved marker extraction to Python; generated exact slices are hashed | Native helpers and tick compile and pass in `c2c6272afcef` |
| Raw `compile loadFile` of commented source slices produced syntax errors | Preprocess each extracted source file and test body before compilation | Native helpers and tick compile and pass in `c2c6272afcef` |
| Scheduled staff GUI call switched namespace unexpectedly after suspension | Invoke the actual GUI in unscheduled `isNil` context, matching UI events | Actual GUI/control enumeration and Side masks passed in `a4a3ba8fe16e` |
| Old-body fixture used the player's remote group and assumed immediate radio membership propagation | Create a local group, wait for membership, record rosters and isolated native removal if needed | Registration, exact killed-body Side removal and original General replacement pass in `c2c6272afcef` |
| Server failure plus a 30-second client grace period truncated the lifecycle check | Increase harness client grace to 90 seconds | Both server and client complete in `c2c6272afcef` |

Cell `468e715589c1-radio-cleanup` completed 58 assertions without script errors, with two failed assertions: old-body General removal and immediate holder-null observation. Its late-addition renewal, attached-holder, budget/absence, stale/current terrain restoration and Kavala protection/attachment/captive predicates passed. This is native helper/tick evidence, not a full restarted Kavala lifecycle test.

The old-body radio failure persisted through the full three-second wait. The isolated native removal afterward succeeded, proving that the supplied body identity and channel were removable. `fn_clientEventRespawn` receives `[newUnit,oldUnit]` and correctly calls `[2,-1,newUnit,oldUnit]`; the fixture's unused channel argument does not affect type-2 routing. The killed callback passes its captured body as the third argument. High Command refreshes access and initial player setup uses membership add/remove paths, so neither changes the four-argument contract. The defect is the same-frame removal/addition sequence retaining the old native roster. The correction separates old-body removal from replacement addition and observes replacement membership before removal; it stops after three seconds or when the old body is gone or has become the current live player again. It does not remove the captured replacement or rewrite desired channel preferences. A second fixture case now starts with an entirely unregistered replacement so ordering changes cannot pass merely because the replacement was already a member.

The holder fixture assumed `isNull` immediately after the production tick's `deleteVehicle`. Bohemia explicitly documents that [object deletion completes on the next frame](https://community.bistudio.com/wiki/deleteVehicle). The corrected fixture checks that production consumed the queued holder and incremented the deletion counter, then waits up to one second for the actual object to become null. No additional holder production change was indicated by this failure; the corrected native tick/deletion assertion passes in `c2c6272afcef`. The same timing correction applies to the damage fixture's rejected-projectile assertion and is tracked as TEST-15 in the central ledger.

A further SYS-07 review tightened the asynchronous retirement worker before retest. The current-player identity check, roster inspection and old-body removal now share an unscheduled transition, so a scheduled suspension cannot separate the safety check from removal. If the replacement dies or is deleted, or an optional subscription is revoked while waiting, old-body removal no longer depends on that replacement joining the channel. The worker still captures body objects and a copied channel list, never grants membership asynchronously, and exits when the old body is the current live player again. Native fixture cases now cover deleted/dead replacements, revoked subscription, and preservation of a current live body, in addition to the original existing-member and fresh-replacement cases. These production lifecycle outcomes pass in `c2c6272afcef`; its setup failures and required isolated rerun are described next.

Cell `c2c6272afcef-radio-cleanup` completes **70 assertions**, with two failed setup preconditions and zero script errors, malformed records, cleanup errors or source/fixture/runtime changes. The original exact old-body General removal, existing-player preservation, fresh-replacement addition/removal, deleted/dead replacement retirement, revoked-subscription retirement and current-live-body preservation outcomes all pass. Every cleanup assertion passes, including late cargo renewal, actual unchanged-holder deletion after the engine frame, attached-holder retention, stale/current terrain restoration and Kavala safety predicates. The cell remains failed because its two preconditions failed; successful outcome assertions cannot substitute for valid setup.

TEST-16 records the fixture isolation defect: successive cases re-added the same old object immediately after observing removal from the asserted channels, while the previous three-second worker could still be completing retirement across its captured channel list. The first transition reused that object between times 30.777 and 30.969; the later failed DEAD setup followed the DELETED result between 31.518 and 31.719. This permits intended old-body retirement to compete with the next case's registration. The old combined preconditions did not record which membership Boolean failed. Each case now creates its own old object, waits up to five seconds for required native membership, and records those Booleans separately. All original assertions remain strict, and the production worker is unchanged. The fresh frozen native radio cell below verifies this correction. Genuine death/respawn, another player identity, VON delivery and the complete Kavala restart/collector lifecycle remain separate acceptance work.

Final focused cell `3e45c5f9b45c-radio-cleanup` passes **70/70 assertions**, including every fresh-object setup and lifecycle outcome, with zero script errors, malformed records, cleanup errors or changed source/fixture/runtime files. This closes the focused radio and cleanup regression checks and TEST-16. The result covers actual client permissions, membership and staff controls plus actual server housekeeping helpers/tick; it does not extend to the separate full mission and human acceptance checks listed above. Earlier failed cells remain preserved.
