# Support remediation, 2026-09-10

This records the corrective passes after `review-support.md`, including defects exposed by native testing and defects in the test fixtures. Final focused cell `b11ac9b1b0a4-support` passes all 207 checks with zero script errors, assertion failures, malformed records, cleanup failures or frozen-source/fixture/runtime changes. It runs a dedicated server and one real graphical player. The normal-placement phase uses the mission's actual three-second core cadence; first construction debits and arms in 6.301 seconds. Full placement UI, distinct-player permissions and live-server acceptance remain separate from this focused result.

## Issue ledger

IDs match the [combined remediation ledger](REMEDIATION_LEDGER.md). Source defects and fixture defects are recorded separately.

| ID | Symptom and root cause | Correction and evidence |
| --- | --- | --- |
| SUP-1 | Support menus never started through registered CfgFunctions initialization: the function accepted INIT while CfgFunctions supplied postInit. Scheduled duplicate initialization could also create duplicate observers. | Normalize the mode, serialize the latch and retain handler/worker identities. Real postInit and repeated scheduled initialization, incapacitation, recovery and control transitions pass in `7b020c8a27aa-support`. |
| SUP-3 | A client success Boolean could grant a loaded mortar without spending its tube; scheduled concurrent requests could exceed reservation policy. | Atomic server admission reserves an exact inventory object, pending mortars stay empty/locked, and the server observes that object's consumption before arming. Six concurrent requests, forged/repeated/late ACK, timeout/reset/false ACK, same-class substitution, normal placement and three-mortar section cap all pass in `b11ac9b1b0a4-support`. |
| SUP-4 | All newly added menu calls supplied a Boolean where BIS expects a notification-class string, generating native script errors. | Pass the empty string. `7249c16a2b31-support` preserved 27 errors; `7b020c8a27aa-support` has zero errors and valid menu IDs. |
| SUP-5 | The first atomic correction recursively called the API under isNil, losing authentic remote-execution metadata and rejecting legitimate scheduled requests. | Capture engine provenance before entering a lexical server closure, including artillery CANCEL. `0f65cd3133b8-support` records the exact metadata loss; authenticated concurrency, debit and cancellation/refund cases pass in `7b020c8a27aa-support`. |
| SUP-6 | A server removeBackpackGlobal command identified a unit, so a delayed network effect could consume a replacement backpack. | An authenticated owner-local DEBIT compares the captured container and removes it atomically; the server independently verifies deletion. Worn debit and delayed swap preserving both the dropped original and replacement contents pass in `429136a53def-support` and `7b020c8a27aa-support`. Direct backpack deleteVehicle probes failed and that alternative was not adopted. |
| SUP-7 | Numeric-target setVariable updated only the client. Comparing the server's absent copy with the next empty status could suppress availability clearing. | Track last publication by body and owner in a server-owned cache. Actual client delivery at activity end, role departure and rejoin passes in `7b020c8a27aa-support`. |
| SUP-10 | Immediate transfer of a newly created mortar failed with owner 0; the client received its nonce but never gained locality, so the real PLACE worker timed out. | Reserve atomically first, retry transfer from the existing core sweep after registration, then send PLACE and accept ACK. Native `2802730db519-support` proves the cause; ownership, pre-handoff ACK rejection and three-second-cadence placement/section tests pass in `b11ac9b1b0a4-support`. |
| TEST-9 | Settlement polling timed out because the fixture invoked TICK inside a client RPC; production correctly rejects remote lifecycle calls. | An independently started local server loop calls actual TICK. The failing `429136a53def-support` cell is preserved; accounting settlement passes after correction in `7b020c8a27aa-support`. |
| TEST-10 | The artillery fixture read a client-targeted status property on the server, producing empty arrays and three secondary indexing errors. | Read the authoritative publication cache and independently wait for actual client receipt. All status delivery checks and the zero-error gate pass in `7b020c8a27aa-support`. |

Additional investigated observations: actual recovery left the fixture player prone, so the test now explicitly returns to the standing/crouched posture required for construction. Admission still failed before SUP-5 was corrected, proving posture was not that failure's cause. Direct deletion probes showed that a dropped backpack retains its original container identity, but did not support using deleteVehicle as a debit primitive. These are test/investigation facts, not additional claimed product corrections.

## SUP-1: lifecycle

The first dedicated/graphical fixture (`7249c16a2b31-support`) also exposed a separate inherited menu-call defect: all new support menu calls supplied Boolean `FALSE` as the fifth `BIS_fnc_addCommMenuItem` parameter. The native function emits `Type Bool, expected String` while reading its notification class. Both production functions now supply the empty string, which suppresses notifications as intended by [Bohemia's function contract](https://community.bistudio.com/wiki/BIS_fnc_addCommMenuItem). The fixture checks returned menu IDs and the runner rejects script errors; the original snapshot remains preserved as failing evidence.

The registered `postInit` mode maps to `INIT`. Initialization enters an unscheduled transition before checking/setting its existing once-only latch, so simultaneous scheduled initialization calls cannot duplicate the observer or event handlers. The two handler IDs and observer handle are retained in `localNamespace` for diagnostics. The normal two-second observer reconciles artillery and mortar menus, death/respawn, revive, role changes, and ping cleanup. Dedicated servers and HCs do not start this client observer.

Client control checks use `isRemoteControlling player`, including the dead-controlled-actor case where `remoteControlled` can become null. Server checks use the mission's replicated control flag and BIS owner markers, with a native query only for a local actor. These markers are integration signals, not an independent anti-cheat guarantee. The artillery worker checks them again during its delays; it does not retain a request-time control snapshot. Locality requirements follow the [Bohemia remote control tutorial](https://community.bistudio.com/wiki/Remote_Control_Tutorial).

The extended fixture exposed another locality assumption in artillery publication. A numeric-target `setVariable` only updates that target, so the server cannot use its own copy of the client status variable as a last-sent cache. That comparison could skip sending an empty status at activity end or role departure. Publication now stores `[owner,status]` in the server activity's `published` map keyed by body netId, and sends whenever that pair changes. Tests compare the expected server publication with the actual player's received status, including cleared states. This follows the [targeted setVariable contract](https://community.bistudio.com/wiki/setVariable).

## SUP-3: resource accounting

Each mortar reservation now records its request kind, admitted backpack object, whether the server initiated debit, and whether a placement acknowledgment was already accepted. All server modes run an unscheduled transition even when a custom client uses scheduled `remoteExec`. UID, body, owner, role, nonce, deadline, section cap and pending-request checks remain enforced.

The diagnostic run `0f65cd3133b8-support` identified the admission failure in the first correction: native `isNil` changed a scheduled remote call from `canSuspend=true, isRemoteExecuted=true, owner=4` to `false,false,0` in the recursive API entry. Role and tube checks remained valid. Both support functions now capture caller provenance directly from the engine before entering a lexical server closure under `isNil`; no request argument supplies those values. This preserves atomicity and authentication together. Artillery CANCEL now participates in that same atomic transition as request debit and watchdog refund, preventing scheduled cancellation from interleaving its refund with another transition.

For TUBE, a positive ACK is an intent to commit construction. The server verifies that the exact admitted backpack is still worn, then sends an authenticated DEBIT operation to its owner's machine. The receiver checks that same object again and removes it with the local `removeBackpack` command in one unscheduled operation. The existing server core waits for that captured backpack object to become null before issuing ammunition; no client success Boolean grants stock. Dropping or substituting a backpack leaves the captured object alive, and a delayed DEBIT targeting it cannot remove a replacement. A repeated ACK never performs another debit or adds another magazine. The local command follows [Bohemia's inventory contract](https://community.bistudio.com/wiki/removeBackpack).

The pending mortar contains no ammunition and is locked. This is necessary because the native carry helper may unhide an object before the server receives its ACK. Once paid, the server sends a single authenticated ARM operation to the turret's owner, which adds exactly eight HE rounds and unlocks the mortar. The free MORTAR request follows the same empty-then-arm path without consuming a backpack; its ten-minute cooldown still survives RESET.

The first algorithm used `removeBackpackGlobal` from the server. Independent review identified a timing window because that command targets the unit, whose backpack could change before the network effect arrives. The second native attempt (`300c69c24e6c-support`) demonstrated that dropping a backpack retains its container identity, but direct `deleteVehicle` probes failed both worn and dropped deletion assertions. That primitive was not adopted. The new DEBIT receiver closes the by-unit timing window by performing comparison and removal together where inventory is local; dedicated/client tests now cover a delayed DEBIT packet after a swap, including the replacement's contents and the original backpack on the ground. Final cell `b11ac9b1b0a4-support` proves ordinary ownership handoff and correct ARM delivery with different hull/turret owners. A forced turret-ownership change between accepted ACK and ARM delivery remains untested; the pending mortar is locked to prevent normal occupation from causing that change before settlement.

Run `429136a53def-support` passed actual DEBIT removal of a worn backpack and preservation of both the dropped original and replacement/FirstAidKit when DEBIT arrived after a swap. It also admitted exactly one of six concurrent scheduled requests with captured owner 4 preserved inside the unscheduled transition. A subsequent fixture defect prevented normal settlement polling: its test RPC attempted to invoke TICK within a remote context, which the production API correctly rejected. The fixture runs production TICK from its independent local server loop every 0.25 seconds during adversarial cases, then switches to the product's existing three-second core cadence for ordinary placement and section-cap cases. This historical run's wait failures are not a passing end-to-end accounting result.

Cancellation has an explicit boundary: a rejected, false, expired or reset request **before the server accepts its handoff** consumes no tube. An accepted handoff commits the tube cost; later reset/death/role departure retires that equipment without refund, as with a deployed mortar. If inventory deletion is not observed before the deadline, the mortar retires empty. No speculative refund creates another tube while a delayed removal remains possible. The protocol prevents this endpoint from minting mortars through a forged acknowledgment; it cannot make all Arma client-local inventory commands cheat-proof.

## Focused fixtures

Files under `tests/ia-update/support-*` require one Windows dedicated server and a real graphical player. Register the test-only `QS_fnc_iaSupportTest` endpoint with `allowedTargets=2`, compile `support-dependencies.sqf` on both processes, and retain the actual production support CfgFunctions and artillery postInit registration. The common harness supplies `IA_fnc_assert`, `IA_fnc_log` and `IA_fnc_finish`. Do not package these test files into the production mission.

The test replaces external mission dependencies only: safe-zone lookup, messaging, data-list lookup, and the placement UI handoff. The handoff uses actual `attachTo`; a controllable wait lets adversarial requests run while the real PLACE worker is pending. Inventory objects, locality transfer, production REQUEST/ACK/RESET/TICK branches, server role registry, native backpacks and turret magazines are real engine paths. This fixture does not claim to validate the full placement UI, revival UI, Robocop, or a second distinct Steam identity.

Cases cover postInit idempotence, six concurrent scheduled requests, empty pending mortars, incorrect nonce, forged success without client-side inventory removal, duplicate/late ACK, tube reuse, false ACK, reset/timeout before handoff, substitution with an identical-class backpack, the real PLACE worker's ACK, three-section admission, fourth-tube preservation, and free-request cooldown preservation. Client/server coordination failures are assertions and the server has a bounded completion deadline.

The first native attempt completed 38 checks with one assertion failure and 27 script-error captures. PostInit idempotence, menu removal/restoration through actual incapacitation and native control entry/exit passed their state assertions, but the menu calls still emitted the API error above. The inventory phase stopped when no reservation was admitted, so that attempt provides no debit evidence. The second fixture explicitly returned the recovered player to standing and still failed admission with HEALTHY/STAND state; posture therefore does not explain that failure. The current diagnostic mode (`QS_mortarSupport_diagnostics`, false by default) records remote execution context around serialization and authorization facts without logging UIDs, and the fixture retains denial messages.

The extended artillery fixture executes actual REQUEST/CANCEL and locally queued server START/END/WATCHDOG calls. It checks FO stock reservation and exact pending-round refund, replayed cancellation, stale epochs, repeated activity start without refill, replacement of a pending worker by the next activity, and rejection of an old activity's END. JTAC executes one real empty-area strike through its production worker, then checks that requesting an eight-bomb volley spends only one, and that role rejoin or duplicate registry rows for the same player UID cannot refill that UID's spent share or increase the activity's fixed issuance. It does not substitute a second distinct identity for the single graphical client.

Cell `2802730db519-support` passes actual START assertions that LASER/IR resolve to their configured shotMissile children, plus an actual BOMB_HE release-time rejection: the player moves onto a target that was safe at admission, the worker rejects its newly danger-close release, and the test verifies one refund without another watchdog credit.

## SUP-10: deferred ownership handoff

In `7b020c8a27aa-support`, normal PLACE fails its eight-second readiness wait, then sends a false ACK and cancels. Custom ACK cases execute the actual server debit and ARM paths successfully. Five assertions fail: normal construction, second and third section entries, the resulting fourth-tube cap case, and free-mortar construction. Diagnostic cell `2802730db519-support` establishes the source defect: every creation-time transfer returned false with owner/turretOwner 0; the client's surviving mortar had the correct nonce, role and range but local=false. The fixture attach dependency was never reached.

REQUEST now reserves its slot and exact backpack before returning. The existing core sweep retries ownership transfer only after the engine reports a nonzero owner and only while the connected body, role, nonce, readiness and deadline still match. Once transfer succeeds, a server-owned handoff flag prevents repeat sends and enables ACK admission. The original reservation bounds transfer to 15 seconds; a single successful server handoff starts a fresh 15-second placement/debit window so the three-second core cadence cannot consume the client's eight-second locality allowance. No client packet can restart either window. There is no new per-request worker, and failed transfer retains the empty/locked state until retirement.

The updated fixture waits for server handoff before its adversarial ACK tests, asserts the resulting server-observed owner, and switches its independently started TICK loop to the actual three-second cadence for ordinary placement and section-cap cases. It also sends an ACK before handoff to verify that an admitted slot alone cannot debit a tube. All pass in `b11ac9b1b0a4-support`. Its server observed mortar owner 4 and turret owner 2: PLACE correctly ran on the player's machine while ARM correctly ran on the server-owned turret. The first normal construction completed in 6.301 seconds; second/third mortar admission, fourth-tube preservation and free-request inventory/cooldown preservation passed. Earlier artifacts remain unchanged.

Independent source review after the native pass found no additional defect in the deferred handoff. It checked reservation-before-transfer, captured-auth atomic dispatch, one-time handoff/deadline transition, pre-handoff ACK rejection, readiness/body/nonce guards, exact-container debit and ARM routing to the separately owned turret. No source change was requested.

## SUP-2 and SUP-8: native projectile evidence

SUP-2 is now engine-confirmed. `b5094ad22f2c-damage` captured real friendly projectile events with source objNull and a valid WEST instigator; the production foot modifier returned 0.925. Supplying the caller as both shot parents produced the intended zero. The production worker now supplies both parents, and the owning damage handlers also resolve surviving instigator information. Corrected HE, rocket, bomb and cluster events passed protection checks in `1415253202bc-damage`. See [damage remediation](damage-remediation.md) for scope and remaining coverage.

Those probes exposed SUP-8 independently: vertically created LASER/IR carrier shells produced nonfinite native children and no impact. `fca96e76c6f3-guided` compared carrier and configured direct-child variants; the direct shotMissile children produced normal impacts and protected friendly damage. The artillery START path now resolves and validates those configured children. The support fixture's START checks establish actual activity configuration; guided impact evidence comes from the dedicated damage matrix.

SUP-9 was a separate native timing issue: ordinary immediate IR target assignment intermittently rejected an eligible enemy designation on the projectile's first frame. Production uses forced assignment after its existing authoritative designation checks and requires that a guided target is still live and non-null inside the atomic release. Failed assignment retains the pending round for refund and deletes the projectile. The exact-release fixtures cover both a dead target and a designation that becomes null between lookup and release, with a next-frame deletion wait rather than an invalid immediate objNull assumption.

Final dedicated cell `f57c8d30a524-damage` passes 98 checks with zero script errors against artillery release source identical to the final passing `b11ac9b1b0a4-support` cell and the current checkout. It covers all eight profiles' accepted releases, finite flight, native impacts and friendly-foot protection, plus dead/missing guided-target rejection without spending a round. It uses server-local AI victims and does not establish real-player Robocop/Zeus UI behavior. See the [guided investigation](guided-native-investigation.md) for the full comparison evidence and fixes.
